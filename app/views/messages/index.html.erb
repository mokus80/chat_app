<h1>Chat</h1>

<ul id="chat">
  <% @messages.each do |message| %>
  	<%= message.content %>
  	<% end %>
</ul>


<%= form_for Message.new do |f| %>
  <%= f.text_field :content %>
  <%= f.submit "Send" %>
<% end %>
